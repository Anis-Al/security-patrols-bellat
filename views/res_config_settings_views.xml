<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.security_patrol</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="99"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Tournées de sécurité" string="Security Patrol" name="security_patrol">
                    <block title="Round Execution" name="round_execution_settings">
                        <setting id="round_sequential_scan" help="Enforce strict order for scanning checkpoints">
                            <field name="enforce_sequential_scans"/>
                        </setting>
                        
                        <setting id="round_timing" string="Grace Period" help="Delay before marking a round as late">
                            <div class="content-group">
                                <div class="mt16">
                                    <field name="round_grace_period" class="o_light_label me-2" widget="integer"/> minutes
                                </div>
                            </div>
                        </setting>
                        
                        <setting id="round_distance" string="Max Checkpoint Distance" help="Maximum allowed distance (in meters) between agent GPS and checkpoint GPS when scanning">
                            <div class="content-group">
                                <div class="mt16">
                                    <field name="max_checkpoint_distance" class="o_light_label me-2" widget="integer"/> meters
                                </div>
                            </div>
                        </setting>
                    </block>
                    
                    <block title="Incidents" name="incident_settings">
                        <setting id="incident_reporting" string="Incident Notification" help="Recipient for incident alerts">
                            <div class="content-group">
                                <div class="mt16">
                                    <field name="incident_notification_email" class="o_light_label" placeholder="security@example.com"/>
                                </div>
                            </div>
                        </setting>
                    </block>

                    <block title="Evidence &amp; Photos" name="evidence_settings">
                        <setting id="evidence_selfie" help="Require selfie for each checkpoint">
                            <field name="require_selfie"/>
                        </setting>
                        <setting id="evidence_place" help="Require photo of place for each checkpoint">
                            <field name="require_photo_place"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_ts_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'security_patrol'}</field>
    </record>


</odoo>
